{
  "buildCommand": "cd team-project && npm ci && npx prisma generate && npm run build",
  "outputDirectory": "team-project/.next",
  "installCommand": "cd team-project && npm install",
  "framework": "nextjs",
  "regions": ["iad1"]
}
